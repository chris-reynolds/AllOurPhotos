name: photo_subset
description: Create a smaller development subset of the photo collection.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
